# Лаборатори №3 — Чанарын сценарио → SLO → k6 threshold

**Оюутны нэр:** Г.Лувсанжал
**Код:** B242270016
**k6 version:**

    $ k6 version
    k6 v2.2.0 (commit/00a9a1b7f5, go1.26.5, linux/amd64)

---

## 1. Чанарын сценариуд (6 хэсэгтэй формат)

### 1.1 Performance сценарио — /cart/add хариу хугацаа

| Хэсэг | Агуулга |
|---|---|
| Тойм | /cart/add endpoint хэвийн ачааллын үед хэр хурдан хариу өгөх ёстойг тодорхойлно. |
| Системийн төлөв | Систем идэвхтэй, ямар нэгэн эвдрэл байхгүй тогтвортой ажиллагаатай үе. |
| Орчны төлөв | Хэвийн ачаалал: 20 VU, 1 минутын турш, localhost сервер. |
| Гадаад өдөөлт | Хэрэглэгч дэлгүүрийн сагсанд бараа нэмэх хүсэлт (POST /cart/add) илгээнэ. |
| Шаардлагатай хариу | Сервер хүсэлтийг богино хугацаанд, алдаагүй (200 OK) боловсруулж хариу буцаана. |
| Хэмжүүр | Хариу хугацааны 95-р перцентиль (p95) < 200 мс байх. |

### 1.2 Reliability сценарио — /pay эвдрэлийн давтамж

| Хэсэг | Агуулга |
|---|---|
| Тойм | /pay endpoint төлбөрийн хүсэлтийг боловсруулахдаа хэр олон удаа алдаа (500) буцааж байгааг хэмжинэ. |
| Системийн төлөв | Систем ажиллаж байгаа, гэхдээ backend заримдаа хугацаа алдаж эвдэрдэг. |
| Орчны төлөв | Хэвийн ачаалал: 20 VU, 1 минутын турш, localhost сервер. |
| Гадаад өдөөлт | Хэрэглэгч захиалгаа төлөх хүсэлт (POST /pay) илгээнэ. |
| Шаардлагатай хариу | Сервер ихэнх тохиолдолд амжилттай (paid: true) хариу буцаах ёстой. |
| Хэмжүүр | POFOD / error rate < 8%. |

### 1.3 Availability сценарио — сервер унаж, дахин асахад

| Хэсэг | Агуулга |
|---|---|
| Тойм | Сервер гэнэт зогсоод 10 секундийн дараа дахин ажилласан үед хүртээмж хэрхэн өөрчлөгдөхийг хэмжинэ. |
| Системийн төлөв | Систем эхлээд хэвийн ажиллаж байгаа, дараа нь гэнэт зогсоод (crash), 10 секундийн дараа дахин асдаг. |
| Орчны төлөв | 10 VU ачаалалтай, 2 минутын турш; тестийн явцад 10 секунд серверийг зориудаар унагаана. |
| Гадаад өдөөлт | Сервер процесс гэнэт зогссон эвдрэл (crash) — хэрэглэгчийн хүсэлтүүд тасралтгүй ирсээр байна. |
| Шаардлагатай хариу | Систем аль болох богино хугацаанд сэргэж, дараагийн хүсэлтүүдэд амжилттай хариу буцаах ёстой. |
| Хэмжүүр | Нийт хугацааны availability >= 90%; сэргэх хугацаа бүртгэгдэнэ. |

---

## 2. SLO хүснэгт

| Сценарио | SLI | Босго | Цонх / нөхцөл |
|---|---|---|---|
| Performance | /cart/add latency (p95) | p95 < 200 мс | 20 VU, 1 мин |
| Reliability | /pay error rate | rate < 0.08 (8%) | 1 мин, 20 VU |
| Availability | бүх хүсэлтийн амжилтын хувь (checks) | rate > 0.90 (90%) | 2 мин, 10с зогсолт орсон |
| (нэмэлт) Performance | /report latency (p95) | p95 < 450 мс | 20 VU, 1 мин |

Босго сонгосон үндэслэл: Сервер /pay дээр санаатайгаар ~5% алдаа гаргадаг тул 8% threshold нь бодит нөхцөлд тохирсон, хэт хатуу биш, хэт сул ч биш босго болно.

Error budget (цагаар): Availability SLO 2 минутын (120 секунд) цонхонд >=90% гэдэг нь 10% буюу 12 секундийн "эвдрэх эрх" (error budget) байна.

---

## 3. k6 threshold тестийн үр дүн

### 3.1 PASS гаралт (results/pass.txt)
- Ажиллуулсан команд: `k6 run slo-test.js 2>&1 | tee results/pass.txt`
- Үр дүн: Бүх 4 threshold амжилттай (PASS) гарлаа:
  - Performance (`/cart/add`): p(95) = 3.06мс (< 200мс босго) ✓
  - Reliability (`/pay`): error rate = 3.78% (< 8% босго) ✓
  - Availability (`checks`): rate = 98.73% (> 90% босго) ✓
  - Нэмэлт Performance (`/report`): p(95) = 393.74мс (< 450мс босго) ✓
- Нийт 2775 HTTP хүсэлт, 925 iteration, 20 VU-аар 1 минутын турш ажиллав.

### 3.2 Chaos тест (results/chaos.txt)
- Ажиллуулсан команд: `k6 run slo-test.js --duration 2m 2>&1 | tee results/chaos.txt`
- Тестийн 25-р секундэд серверийг Ctrl+C-ээр зогсоож, ойролцоогоор 30 секундийн дараа дахин асаав (`node server.js`).
- Threshold-ийн үр дүн:
  - Availability (`checks`): rate = 77.95% → 90% босгыг ХАНГААГҮЙ (FAIL)
  - Reliability (`/pay` error rate): 24.85% → 8% босгыг ХАНГААГҮЙ (FAIL)
  - Performance (`/cart/add` p95): 2.91мс → 200мс босгыг хангасан (PASS)
  - Performance (`/report` p95): 389.15мс → 450мс босгыг хангасан (PASS)
- **Бодит availability (хүсэлтээр тооцсон):** checks_total=5829, checks_succeeded=4544 → 4544/5829 = **77.95%**
- **Тайлбар:** Сервер зогсож байх хугацаанд `/cart/add`, `/report`, `/pay` гурван endpoint зэрэг "connection refused" алдаа өгсөн тул нэг л crash үзэгдэл availability болон reliability хоёр SLO-г зэрэг зөрчлөө. Энэ нь эдгээр хоёр SLO бие биенээсээ бүрэн хамааралгүй биш гэдгийг харуулж байна — доод давхаргын систем (сервер процесс) унахад, өндөр давхаргын бүх метрик зэрэг доройтдог. Performance threshold-ууд (p95 latency) харин PASS хэвээрээ үлдсэн нь сервер ажиллаж байх үед хариу үзүүлэлт хэвийн байсныг илтгэнэ — асуудал зөвхөн downtime-тай холбоотой байсан.
### 3.3 FAIL гаралт (results/fail.txt)
- Ажиллуулсан команд: `k6 run slo-test-fail.js 2>&1 | tee results/fail.txt`
- Санаатайгаар `/cart/add` дээрх performance threshold-ыг бодит боломжгүй хэмжээнд хатууруулав: `p(95)<1` (1 миллисекунд).
- **FAIL болсон мөр:** `http_req_duration{name:cart}` — `'p(95)<1' p(95)=2.7ms` — заасан 1мс-ээс бодит p95=2.7мс их байсан тул FAIL.
- Бусад 3 threshold (checks 98.01%, report p95=390.8мс, pay error rate 5.94%) хэвийн PASS байв — зөвхөн зориудаар хатууруулсан нэг threshold FAIL болсон.
- **Exit code:** k6-ийн жинхэнэ exit code (`${PIPESTATUS[0]}` ашиглан авсан) = **99**. Энэ нь k6-ийн стандарт "threshold зөрчигдсөн" exit code бөгөөд CI/CD дээр pipeline-ийг зогсооход ашиглагддаг.

---

## 4. Дүгнэлт
Энэ лабораторийн ажлын хамгийн хэцүү хэсэг нь чанарын шаардлагыг ("хурдан байх ёстой", "найдвартай байх ёстой" гэх мэт) тоон хэмжигдэхүйц SLI/SLO рүү хөрвүүлэх байсан — учир нь subjective ойлголтыг p95, error rate шиг тодорхой тооцоолж болохуйц хэмжүүр болгоход тохирсон threshold сонгох шаардлагатай байлаа. Chaos туршилт нь миний availability сценариог батласан ч, санаанд оромгүй нэг зүйлийг илрүүлсэн: сервер унасан үед зөвхон availability төдийгүй reliability threshold (/pay error rate) мөн зэрэг зөрчигдсөн, учир нь эдгээр хоёр SLO бие биенээсээ бүрэн тусдаа биш байв. Хамгийн эмзэг SLO нь Availability байсан — 90% threshold-той байсан ч ердөө 77.95% болж унасан нь bounded downtime-ийн нөлөө хэр их байдгийг харуулав. Харин Performance хоёр threshold (/cart/add, /report) chaos тестийн үед ч PASS хэвээр үлдсэн нь сервер идэвхтэй ажиллаж байх үед хариу үзүүлэлт тогтвортой байсныг илтгэнэ. FAIL сценарио хийхдээ threshold-ыг бодит боломжгүй хэмжээнд (p95<1мс) хатууруулах нь k6-ийн exit code механизмыг ойлгоход тустай байлаа — учир нь exit code 99 нь CI/CD pipeline дээр automation-ийг зогсооход ашиглагддаг гол механизм юм. Дараагийн удаа би error budget-ийг зөвхон цагаар биш, хүсэлтийн тоогоор давхар тооцож эхнээс нь харьцуулах бол илүү нарийн дүн шинжилгээ хийж болно гэдгийг ойлгосон. Мөн chaos тестийн үргэлжлэх хугацааг (server downtime) илүү нарийн, автоматжуулсан хэлбэрээр (жишээ нь script ашиглан яг 10 секунд гэж тохируулах) хийвэл илүү давтагдах чадвартай туршилт болно байсан. Ерөнхийдөө энэ даалгавар нь SLO-г зөвхөн онолын хэмжүүр биш, бодит k6 threshold болгож хэрэгжүүлэх практик дадлага өгсөн.
